export const getName = (): string => {
  return "Firebase" + "";
};

export const getAge = (): number => {
  return 38;
};

export const getSum = (a: number, b: number): number => {
  return a + b;
};
